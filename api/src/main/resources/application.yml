spring:
  profiles:
    group:
      local:
        - app
        - domain
      dev:
        - app-dev
        - domain-dev
    active: local

  config:
    import: classpath:application-dev.yml

management:
  endpoints:
    web:
      exposure:
        include: prometheus

logging:
  discord:
    webhook-url:
