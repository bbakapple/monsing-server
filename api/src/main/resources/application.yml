spring:
  profiles:
    group:
      local:
        - app
        - domain
      dev:
        - app-dev
        - domain-dev
    active: local

management:
  endpoints:
    web:
      exposure:
        include: prometheus

---

spring:
  config:
    activate:
      on-profile: local

aws:
  cloudfront-url: https://sadniasdksmkadmiqiq.com

logging:
  discord:
    webhook-url:
